<h1>Tasks</h1>

<ul>
  <% @tasks.each do |task|  %>
    <li><%= link_to task.title, task_path(task) %>, Completed: <%= task.completed %> ><%= link_to 'Destroy', task, method: :delete, data: {confirm: 'Are you sure?'} %></li>
  <% end %>
</ul>

<%= link_to "Add a new Task", tasks_new_path %>
